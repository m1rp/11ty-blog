{% extends "layouts/base.njk" %}

{% block content%}
<section class="intro">
      {{ content |safe }}
</section>

<section>  
  <h2>Latest posts</h2>
    {% for post in collections.posts %}
    {% if loop.index0 < 3 %}
      {% include 'partials/article-card.njk' %}
      {%endif%}
  {% endfor %}
</section>

{# <section>
  <h2> WEBMENTIONS</h2>
  <pre>
  {% for e in webmentions %}
    <a href="{{e.url}}">{{e['mention-of']}}</a>
    {%endfor%}
  </pre>
</section> #}

<section>
  <h2>Right now I'm reading:</h2>
{%- set bookList = reading -%}
{% include "partials/books.njk" %}
<h4><a href="/bookshelf/">Check out</a> what else I've read this year!</h4>
</section>

{% endblock content%}
